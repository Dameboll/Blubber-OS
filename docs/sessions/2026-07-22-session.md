# Session Summary
Date: 2026-07-22 (overnight ~23:50 → 05:10 ET)
Projects touched: blubber-os (branch fix/e2e-suite-green)
Primary focus: Launch-shaping — two-tier split locked, polish batches, voice + settings, persistence sync

---

## What Happened

### blubber-os
Locked the launch philosophy: ONE codebase, kit marker (`~/.claude/.blubber-kit.json`) as the paywall switch. Free tier is a raw shell (demo mode deleted, one scan card, one tip, placeholder stats until connect). Starter Kit gets tour + soul interview + installers, all detection-triggered. Ran three multi-agent workflow batches (with freeze tags before each) covering: raw-shell split, quest library expansion (34 quests / 15 chains), soul interview, Apple-Music-style music library with ID3 cover extraction, tour 3D-hide, deterministic project icons, voice synth + real Settings tabs. Voice ships muted by default. Everything committed through d93fbc4.

---

## Key Decisions
1. One codebase, kit-marker gating — two separate builds rejected (maintenance cost).
2. Free tier gets ZERO guided features — all hand-holding behind paywall (Dame's explicit philosophy).
3. Demo mode deleted entirely — placeholder-until-connected replaces it.
4. First workspace connect zeroes stats baseline — every user gets true fresh start.
5. Voice ships muted:true default — synth reads as "robot noises", opt-in from Settings.
6. Settings tabs: real or cut — AI&Model tab cut, rest wired to prefs-store.

## What Got Built
- Kit marker + detection + tour trigger — complete
- Raw-shell onboarding rewrite — complete
- 34 quests / 15 chains (Veteran + Blubber Bond added last) — complete
- Soul interview (8 questions, all skippable, kit-gated) — complete
- Music library (playlists, sorting, ID3 covers, uploads) — complete
- Blubber voice synth + voice-store + Settings Voice rail — complete (muted default)
- Project icon mapping, Academy art, startup video (compressed 83%), Electron quit fix — complete

## Problems Encountered
- "Install Claude for me" dead → missing setWindowOpenHandler in Electron — resolved
- Demo mode crash → orphaned node server on port 3000; taskkill /T tree-kill in before-quit — resolved
- Demo mode leaking live session data on 5 routes — gated, resolved
- Slop hook blocked tour CSS (layout-prop transitions) — rewrote to snap positions
- Project icons "random" twice — root cause was folder-image thumbs as main visual; replaced with icon tiles

## Open Questions Going Into Next Session
- Academy landing-page URL — Dame to supply (waitlist form is placeholder)
- Voice styles ear-test — Dame hasn't picked a favorite (shipped muted)

## Unfinished (carry forward)
See docs/next-session.md — e2e suite is the launch gate.

---

## Session Health
PRODUCTIVE — big session (~5h, 3 workflow batches). Everything committed, tagged, persisted.
